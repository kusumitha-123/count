#include <stdio.h>
int main() {
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    for(int i=num; i<=num*10;i++)
    {
        printf("%d\n",i);
        i=i+num-1;
    }
    return 0;
}
